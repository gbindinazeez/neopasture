<section class="who-its-for">
  <h2>Who it's for</h2>
  <p>
    NeoPasture is built for individuals and small farmers, not large commercial operations. If you
    want to own livestock without the land or time it usually takes, this is for you.
  </p>
</section>

<style>
  .who-its-for {
    padding: var(--space-7) var(--page-margin);
    max-width: var(--page-width);
    margin: 0 auto;
    border-bottom: 1px solid var(--color-rule);
  }

  .who-its-for h2 {
    font-size: var(--font-h2);
    max-width: 22ch;
  }

  .who-its-for p {
    margin-top: var(--space-4);
    max-width: var(--measure);
  }
</style>
