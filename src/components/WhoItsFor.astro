<section class="who-its-for">
  <h2>Who it's for</h2>
  <ol>
    <li>
      <span class="who-its-for__index">01</span>
      <div>
        <h3>Livestock Owners</h3>
        <p>
          Individuals who want to own livestock while NeoPasture handles the feeding, care, and
          monitoring.
        </p>
      </div>
    </li>
    <li>
      <span class="who-its-for__index">02</span>
      <div>
        <h3>Farm Partners</h3>
        <p>Existing farms looking for digitisation, monitoring, and operational tools.</p>
      </div>
    </li>
    <li>
      <span class="who-its-for__index">03</span>
      <div>
        <h3>Enterprise Partners</h3>
        <p>Large farms, cooperatives, agribusinesses, and institutions.</p>
      </div>
    </li>
  </ol>
</section>

<style>
  .who-its-for {
    padding: var(--space-7) var(--page-margin);
    max-width: var(--page-width);
    margin: 0 auto;
    border-bottom: 1px solid var(--color-rule);
  }

  .who-its-for h2 {
    font-size: var(--font-h2);
    max-width: 22ch;
  }

  .who-its-for ol {
    list-style: none;
    margin: var(--space-6) 0 0;
    padding: 0;
    display: flex;
    flex-direction: column;
  }

  .who-its-for li {
    display: flex;
    gap: var(--space-5);
    padding: var(--space-5) 0;
    border-top: 1px solid var(--color-rule);
  }

  .who-its-for li:first-child {
    border-top: none;
  }

  .who-its-for__index {
    font-size: var(--font-small);
    color: var(--color-primary);
    font-weight: 700;
    flex: 0 0 2.5rem;
  }

  .who-its-for h3 {
    font-size: var(--font-h3);
  }

  .who-its-for p {
    margin-top: var(--space-2);
    color: var(--color-ink-muted);
  }
</style>
